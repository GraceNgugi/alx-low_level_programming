#include "main.h"

/**
 *add - adds two integers and returns the result
 *@a: is the character to be checked
 *@b: is the character to be checked
 *Return: the last digit
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
