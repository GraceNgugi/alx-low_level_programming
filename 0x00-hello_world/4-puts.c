#include<stdio.h>
/**
 *main -serves as an entry point for program execution
 *return : always0 meaning the output is successful
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
